services:
  app:
    build: .
    container_name: kabanbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/messages.db
    volumes:
      - ./data:/app/data
